---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./qwen3.8-27b.yaml # GPU 0: general / coding / vision
  - ./gemma-4-31b-it-qat.yaml # GPU 1: reviewer lane
  - ./embed.yaml # CPU on the MS01s: embeddings
  - ./rerank.yaml # CPU on the MS01s: reranker for memini recall
